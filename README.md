# Kanban

**Kanban** — учебный Java-проект по реализации менеджера задач, основанного на системе Канбан с возможностью взаимодействия через HTTP API.

## 📋 Описание

Приложение позволяет создавать, изменять и удалять задачи, эпики и подзадачи, отслеживать историю взаимодействий, а также управлять приоритетами задач. Проект реализован с использованием принципов ООП, SOLID и покрыт модульными тестами. Взаимодействие с системой реализовано через HTTP API с использованием формата JSON.

## ⚙️ Функциональность

### 🧩 Работа с задачами

- CRUD-операции над:
    - Обычными задачами (`Task`)
    - Подзадачами (`Subtask`)
    - Эпиками (`Epic`)
- Автоматическое обновление статуса эпика на основе подзадач
- Поддержка времени начала и длительности задач, предотвращение пересечений

### 💾 Хранение данных

- `InMemoryTaskManager` — хранение в памяти
- `FileBackedTaskManager` — сохранение и восстановление из файла (CSV, RFC 4180)

### 🌐 HTTP API

Реализован сервер `HttpTaskServer` с REST-подобным API:

| Метод  | Эндпоинт               | Описание                             |
|--------|------------------------|--------------------------------------|
| GET    | `/tasks`               | Получить все задачи                  |
| POST   | `/tasks`               | Создать новую задачу                 |
| POST   | `/tasks/{id}`          | Обновить задачу по ID                |
| DELETE | `/tasks/{id}`          | Удалить задачу по ID                 |
| GET    | `/subtasks`            | Получить все подзадачи               |
| POST   | `/subtasks`            | Создать новую подзадачу              |
| POST   | `/subtasks/{id}`       | Обновить подзадачу по ID             |
| DELETE | `/subtasks/{id}`       | Удалить подзадачу по ID              |
| GET    | `/epics`               | Получить все эпики                   |
| POST   | `/epics`               | Создать новый эпик                   |
| POST   | `/epics/{id}`          | Обновить эпик по ID                  |
| DELETE | `/epics/{id}`          | Удалить эпик по ID                   |
| GET    | `/epics/{id}/subtasks` | Получить подзадачи конкретного эпика |
| GET    | `/history`             | Получить историю просмотров          |
| GET    | `/prioritized`         | Получить список задач по приоритету  |

### ✅ Обработка ошибок

- `200 OK` — успешное получение данных
- `201 Created` — успешное создание/обновление данных
- `400 Bad request` — пересечение по времени задач, неправильно сформированный запрос
- `404 Not Found` — объект не найден
- `500 Internal Server Error` — внутренняя ошибка сервера

## 🧪 Тестирование

- Покрытие unit-тестами всех обработчиков HTTP API
- Отдельные тест-классы:
    - `HttpTaskHandlerTest`
    - `HttpSubtaskHandlerTest`
    - `HttpEpicHandlerTest`
    - `HttpHistoryHandlerTest`
    - `HttpPrioritizedHandlerTest`
- Базовый класс `HttpBaseTest` с общей логикой запуска и остановки сервера

## 🚀 Установка и запуск

```bash
git clone https://github.com/ваш-юзернейм/java-kanban.git
```

Открыть в IntelliJ IDEA или другой IDE, запустить `Main.java` или `MainHttpServer.java`.

> Требуется Java 11+

## 🔮 Планы по развитию

- Хранение задач в базе данных (SQL)
- Веб-интерфейс для управления задачами
- JWT-аутентификация для API
- Поддержка WebSocket для обновлений в реальном времени

## 🧑‍💻 Точка входа

- Для CLI: `Main.java`
- Для HTTP API: `MainHttpServer.java`

## 📅 История изменений

См. [CHANGELOG.md](CHANGELOG.md)